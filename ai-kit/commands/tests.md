---
description: Write unit tests for existing code, by this repository's conventions.
---

Run the `write-tests` skill.

Target: $ARGUMENTS

Follow its SKILL.md: resolve where this package keeps its tests before writing one, delegate to
the `write-tests` agent, and never change the code under test to make a test pass.
