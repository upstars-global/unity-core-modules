---
description: Add or clean translation keys the way the Lokalise flow requires.
---

Run the `i18n-keys` skill.

Keys or context: $ARGUMENTS

Follow its SKILL.md: new keys go into `en.json` only — the other locales arrive from Lokalise
after the MR, and writing them by hand is what breaks the flow.
