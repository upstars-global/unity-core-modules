---
description: Check and move code into unity-core-modules so both twins share it.
---

Run the `extract-to-core` skill.

Candidate: $ARGUMENTS

Follow its SKILL.md: run the blocker check first and stop on a blocker — a `.vue` file cannot go
to the library, and neither can code that needs an app-only alias.
