---
description: Write or refresh knowledge pages for changed or hot code.
---

Run the `update-docs` skill.

Scope, if narrowed: $ARGUMENTS

Follow its SKILL.md: read the doctrine first, then write only pages that carry what the code
cannot, and finish with `--rehash`, `--build-index` and `--lint`.
