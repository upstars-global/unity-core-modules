---
description: Scaffold a component with its test, story and translation keys.
---

Run the `vue-component` skill.

Target: $ARGUMENTS

Follow its SKILL.md: settle the package and the name first, scaffold with the script so every
file lands where this package keeps it, and do not overwrite anything that exists.
