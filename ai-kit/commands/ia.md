---
description: Impact analysis for the current branch — which pages a manual tester must open.
---

Run the `impact-analysis` skill for the current branch.

Follow its SKILL.md exactly: collect the evidence with the script, delegate to the
`impact-analysis` agent, return the report verbatim, and only then offer to write it into Jira.
