export enum EnumLootboxState {
    issued = "issued",
    activated = "activated",
    canceled = "canceled",
    expired = "expired",
}

export enum Mode {
    Lite = "rocket_lite",
    Pro = "rocket_pro",
    Max = "rocket_max"
}


export enum GiftLevelTypes {
    Currency = "bonus",
    Freespins = "freespins",
}
